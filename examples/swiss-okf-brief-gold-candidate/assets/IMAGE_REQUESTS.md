# Image Requests

No AI-generated image assets were used.

This example is a deterministic HTML/CSS Swiss OKF Brief card deck. The visual proof is represented through text, directory trees, YAML frontmatter, and structured proof rows.

The source visual direction comes from user-supplied reference images of a high-performing OKF card deck. Those images are treated as design/source references, not copied bitmap assets inside the rendered cards.

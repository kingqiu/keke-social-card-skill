# Image Requests

No new image-generation request was needed for this candidate.

## Proof Assets

Source route: native HTML/CSS proof modules in `index.html`.

Layout intent:

- Preserve a 3:4 portrait card rhythm.
- Use a phone-style result panel on the cover.
- Use a result-first card module on the before/after page.
- Use native cover/package/copy modules on the save-value page.

Portrait-fit decision:

- This candidate does not use external proof images.
- If a future Rednote Native candidate references a landscape source image, redraw the source illustration into a portrait-friendly proof asset before inserting it into the template.
- Use GPT Image 2 or the configured raster image model for the inserted illustration itself when a redraw is needed; do not redraw the whole template frame just to fix one landscape illustration.

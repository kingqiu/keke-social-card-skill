# Asset Sources

No external proof bitmap is used in this candidate.

| Asset | Source | Role |
|---|---|---|
| Native proof panels | `index.html` HTML/CSS | Phone proof, result-first card, publishing package, copy package |

The Rednote Native Gold reference must prove its own visual system. Result/proof areas are built as native card modules instead of borrowing Lookbook Journal screenshots.
